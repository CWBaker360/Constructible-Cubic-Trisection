# Paper Source

This folder contains the LaTeX source for:

> **A Constructible Cubic Error Cascade for Approximate Angle Trisection**

Main file:

```text
constructible_cubic_trisection.tex
```

Suggested compile command:

```bash
pdflatex constructible_cubic_trisection.tex
pdflatex constructible_cubic_trisection.tex
```

The generated PDF may be added here or to a separate `release/` folder after final compilation.
